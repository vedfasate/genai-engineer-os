# Components

Components should be small, reusable, and accessible.

- `src/shared/ui/` contains primitive UI elements
- `src/widgets/` contains larger, composed user interfaces
- `src/features/*/components/` contains feature-specific UI

Avoid deeply nested props and keep component APIs intentional.
